apiVersion: v1
kind: Namespace
metadata:
  name: eido
  labels:
    app.kubernetes.io/part-of: madfam-platform
    enclii.dev/type: application
    # Cosign keyless verification (GH Actions OIDC) — CI signs every image.
    enclii.dev/verify-signatures: "true"
